services:
  - name: vitsmail
    env: node
    region: oregon
    buildCommand: npm install
    startCommand: npm start
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: "3000"
      - key: DOMAIN
        value: vitsmail.sryze.cc